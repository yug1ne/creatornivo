# Early Access status banner — remove after full launch

1. Delete `src/components/layout/early-access-status-banner.tsx`
2. Remove import + `<EarlyAccessStatusBanner />` from:
   - `src/app/(protected)/layout.tsx`
   - `src/app/(public)/page.tsx`
3. Optional: remove `statusBannerMessage` from `src/config/early-access.ts`

# Pricing promo banner (public homepage only) — separate from status banner
# `src/components/landing/early-access-banner.tsx` — remove when Early Access pricing ends